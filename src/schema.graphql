type Query {
  search(query: String!): [Movie!]!
  movie(id: ID!): Movie!
}

type Movie {
  id: ID!
  title: String!
  description: String
  poster: String
  year: Int!
  countries: [String!]!
  genres: [String!]!
  directors: [String!]
  writers: [String!]
  stars: [String!]
  rating: Float!
}
