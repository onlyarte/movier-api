type Query {
  search(query: String!): [Movie!]!
}

type Movie {
  id: ID!
  title: String!
  description: String!
  year: Int!
  countries: [String!]!
  genres: [String!]!
  directors: [String!]!
  writers: [String!]!
  stars: [String!]!
  rating: Float!
}
